package runners;

import org.apache.logging.log4j.LogManager;
import org.apache.logging.log4j.Logger;
import org.junit.Test;

public class NewTest {
	
	/*
	 * private static final Logger logger = LogManager.getLogger(NewTest.class);
	 * 
	 * private void name() { logger.info("tui la truong");
	 * logger.debug("tui la truong   dqwdqwdqdwq  "); }
	 * 
	 * @Test public void f() { //DOMConfigurator.configure(
	 * "D:\\eclipse-workspace\\newframework\\src\\main\\resources\\log4j.xml");
	 * name(); logger.error("tui la f   error"); logger.warn("tui la f   warn");
	 * logger.debug("tui la f   debug"); logger.trace("tui la f   trace");
	 * logger.fatal("tui la f   fatal  "); }
	 */
}
